# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
      - name: Build site
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: https://nibnmfdtgeanrycmlevf.supabase.co
          NEXT_PUBLIC_SUPABASE_ANON_KEY: sb_publishable_WB3VmTv2VLENrphPeMYL5w_H3-Y0mXO
          NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME: dedmzt6lv
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GDGOC_CIT }}
          channelId: live
          projectId: gdgoc-cit
